/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: [
      "localhost",
      "unsplash.com",
      "images.unsplash.com",
      "stagingapi.mazaady.com",
      "mazaady.com",
    ],
  },
};

module.exports = nextConfig;
